import 'package:flutter/material.dart';

class CustomModel {
  String name;
  num height;
  num width;
  Color color;
  IconData iconData;

  CustomModel({
    required this.name,
    required this.height,
    required this.width,
    required this.color,
    required this.iconData,
  });
}
